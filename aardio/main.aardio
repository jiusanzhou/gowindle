import electron.app;

var mainApp = electron.app("GoWindle");

// start the go or something other processing

mainApp.start({
    // replace the index url while release
    indexUrl = "http://127.0.0.1:1234/installer";
    browserWindow = {
        title = "GoWindle";
        width = 640;
        height = 420;
        minWidth = 560;
        minHeight = 380;
        frame = false;
        webPreferences = {};
    };
});

return win.loopMessage(); 
